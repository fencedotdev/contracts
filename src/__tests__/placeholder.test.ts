import { describe, expect, it } from "vitest";

// Placeholder so CI's test step never fails on an empty suite (Phase 0 checklist 0.2.3).
// Delete once this repo has real tests to run instead.
describe("placeholder", () => {
  it("passes", () => {
    expect(true).toBe(true);
  });
});
