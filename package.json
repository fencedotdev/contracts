{
  "name": "@fencedotdev/repo-template",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vitest/coverage-v8": "^4.0.0",
    "eslint": "^10.8.0",
    "eslint-plugin-security": "^4.0.1",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.65.0",
    "vitest": "^4.0.0"
  }
}
